# r1/ — FL2.1 공개형 제품층 (완성-국면 · [M-95~96])

**지위**: ★커널 무접촉 — FL2.2 커널(`fin_lean/lang22/kernel22.py` v0.1 — [M-127] ·
FL2.1 법 문언-동일 승계 + 잡별-T + H7)을 읽기-전용 임포트하는 서비스층(법 재구현 0). 헌장·수용 게이트 기록은
개발 저장소(비공개)의 R1_PROGRAM 문서이며, 게이트의 실측 판정은 `results/r1_gates.json(게이트 실행 시 생성되는 산출물 — 번들 비-동봉)`
과 `test_r1.py`(누구나 재실행 가능)가 정본이다.
★**화폐 모델 = 자유은행 v0**([M-103] — D-4 확정): 모든 노트에 발행자(색) · join =
자기-IOU 발행 · 상환 = 발행자에게만(색-일치) · 유입 = 상호-신용 스왑(/bootstrap) ·
배상 노트 = 불이행-앵커 색. 유비 근거 = 자유은행 사료 연구(개발 저장소 — 비공개 ·
요지는 RELEASE·QUICKSTART의 화폐-모델 절에 자기완결로 서술).

| 파일 | 무엇 | 게이트 |
|---|---|---|
| `node.py` | HTTP 노드(제출·조회·audit·잡·틱·★호가 창 /board·★범위-선언(조회 = /stats.scopes)·★챌린지 /challenge(POST)) · append-only 대장 + 기동 전체-리플레이 · 경계 예외-격리 · k-of-n 공동-서명 사이드카 | A-3·A-4·A-6 |
| `mcp_server.py` | ★에이전트-네이티브 정문 — 로컬 MCP 서버(도구 34·sdk 래퍼·--selftest — ★원격 실행 금지 = 신뢰 재유입) | A-2 |
| `sdk.py` | ★외부 주체용 클라이언트(커널 무임포트 — 서명·헤드 독립 재구현·골든 결박) · 키 자율 보관 · 라이트 검증 | A-2·A-6 |
| `worker.py` | 앵커 워커 — ★실제 계산-이행(죽으면 커널 시한-사고 실발동) | A-1 |
| `underwriter.py` | ★[M-154] 제3자 자동-인수자(스캔·커버-호가·원자-체결 leg — 정문 = `UNDERWRITING.md`) | P-6 |
| `cosigner.py` | ★D-2 분리 공동-서명자 데몬(비동기 /cosig 회신 — 키 물리 분산) | A-6 |
| `jobs.py` | 검증-대상 사다리: sha256-체인·표본검증·pycheck(협조 한정)·★pyjudge(판정-분리 = v2) | A-1 |
| `package.py`(저장소-측 조립 도구 — 번들 비-동봉) | ★D-9 공개 번들 조립 + 자립성 자기-시험(manifest) | — |
| `test_r1.py` | ★수용 게이트 스위트 37종(골든서명·실물페릴·복구·퍼즈·소크·암호실물·서명치유·★화폐모델·★원자성·★판정분리·★서명분리·가격결박·표본검증·코드이행·인수개방·★해시결박·통계증명·★호가창·★★뿌리·★범위결박·★챌린지창·★★세대[단위·잡별T·H7]·★ERC8004어댑터·★단방향밸브·★암호-확실kind·★수락채널·★출처계기·★★자원소진[H-1]) | 전체 |
| ★`test_adversarial.py` | ⚙️**적대 배터리 10항**([M-188] H-3) — **게이트와 다른 축**: 게이트가 「약속대로 쓸 때 맞는가」를 잰다면 배터리는 **「틀리게·악의로 쓸 때 사는가」**를 잰다(재생·교차세계·도메인 혼동·권한 상승·오용 입력·경계 우회·시빌·슬로우로리스·크기 남용·혼합-kind 부하). ★결과를 **숨기지 않고 기록**한다 — 막히면 ✅, 안 막히면 ⛔ | 전체 |
| `replay_full.py` | ★H7(FL2.2) — 시드-독립 전-상태 공개 재검증 도구 | — |
| `CHECKER_PATTERNS.md` | ★술어 표준형 6 + 거래 레시피 5 + 발견층 관례 | — |
| `EXTERNAL_QUICKSTART.md` | ★외부 참여 문서(맥락-0 실증의 유일 입력) · `VERIFIER.md` 검증자 안내 · `RELEASE.md` 발표문 초안 | A-2 |

## 실행

```bash
python3 r1/test_r1.py                     # 수용 게이트 전량(R1_GATES_PASS)
python3 r1/node.py --data DIR --port 8788 --auto-tick 2       # 노드
python3 r1/worker.py --url http://127.0.0.1:8788 --key DIR/anchor0.key  # 앵커 워커
```

판정 기록 = `results/r1_gates.json(게이트 실행 시 생성되는 산출물 — 번들 비-동봉)`(재실행 = 위 명령).
